class Deck
attr_reader :cards, :category

  def initialize(cards)
    @cards = cards
  end

  def count
    cards.count
  end

  def cards_in_category(cat)
    @cat = cat
  cards.each do |card.category|
    if card.category = @cat
  category_count += 1
  category_count
    end
  end
  end
end
